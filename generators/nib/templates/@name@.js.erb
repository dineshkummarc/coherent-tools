/*jsl:import coherent*/

NIB('<%=name%>', {

  '<%=name%>': VIEW({
      /* Put your view structure here. */
      
      ':root': coherent.View({
              /* This is the container DOM element. */
          })
  }),

  'owner': {
    view: REF('<%=name%>')
  }

});
