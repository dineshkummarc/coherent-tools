/*jsl:import coherent*/

NIB('<%=name%>', {

    'custom-view': VIEW({
                    /* Put your view structure here. */
                    
                    ':root': coherent.View({
                            /* This is the container DOM element. */
                        })
                }),

    'owner': {
        view: 'custom-view'
    }

});
